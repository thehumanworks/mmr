doff notes for the next session

Also update Linear comments at ticket boundaries with:

- scope completed
- tests added
- commands run
- remaining risks
- downstream dependencies
- next recommended ticket

At the end of every substantial work session, write a compact resume packet in the progress file and final response:

- “Current state”
- “Last verified state”
- “Next exact action”
- “Do not redo”
- “Watch-outs”

When resuming:

1. Read the progress file first.
2. Check `git status`.
3. Pull current Linear ticket state.
4. Read only the relevant specs/rules/code for the active ticket.
5. Continue from the recorded next exact action.
6. Avoid rediscovering context unless the progress file is stale or contradicted by code.

Escape Hatch:
Do not stop just because something is ambiguous, difficult, slow, or imperfect. Make reasonable assumptions, document them, and continue.

Only break and ask the user when there is truly no responsible way to proceed, such as:

- credentials or external permissions are required and no mock/local path can continue
- two requirements directly contradict each other and choosing would cause irreversible architecture or data-loss risk
- destructive action is required
- Linear/repo state is unavailable and local progress cannot continue safely
- repeated verification failure points to a product decision rather than an implementation bug

Before using the escape hatch:

1. Try a local/mock/offline path.
2. Search repo docs/specs/ADRs/Linear for the answer.
3. Pick the safest reversible assumption if possible.
4. Record the assumption and continue.
5. If blocked, provide the smallest possible question plus the exact options and consequences.

Default posture:
Keep moving. Prefer reversible, well-documented progress over waiting. Preserve user work. Leave a trail clear enough that any future session can resume without needing memory of this chat.Use Linear and the local repo at /Users/tomasroda/projects/mmr to deliver the `mmr Memory Fabric MVP` end to end.

Goal:
Proactively, independently, and with senior-engineering quality, turn all Todo Linear issues in the `mmr Memory Fabric MVP` project into a coherent shipped MVP. Treat this as one product delivery effort, not disconnected ticket work. Optimize for durable architecture, tests, docs, release evidence, and continuity across many sessions.

Start by pulling the current Linear Todo backlog for the project and using Linear as the source of truth.

Known ticket spine:
NHL-268 architecture/schema/verification contract
NHL-269 SQLite/libSQL local store and migrations
NHL-270 source adapter framework, watcher, reconciler
NHL-271 `mmr note`
NHL-272 redaction-before-sync
NHL-273 `mmr rg`, `mmr search`, result citations
NHL-274 Codex importer/capture
NHL-275 Claude Code importer/capture
NHL-276 Cursor importer/capture
NHL-277 `mmr link`, `sync`, `status`, default GitHub `mmr-store`
NHL-278 dream runner/provider abstraction
NHL-279 `mmr dream` assimilation workflow
NHL-280 integrated CLI UX, diagnostics, docs
NHL-281 end-to-end release verification
NHL-282 rename `remember` to `summary` while preserving raw retrieval

Critical path:

1. Complete NHL-268 first. Lock architecture, schema, CLI contract, fixtures, dependency graph, and verification contract.
2. Build the storage spine with NHL-269.
3. Parallelize capture/search/summary/note work once the contract is stable.
4. Add redaction and sync safety before remote/hydration behavior.
5. Add provider-neutral dreaming and evidence-linked learned memory.
6. Finish with integrated CLI UX, docs, adversarial review, and NHL-281 release gate.

Parallelization:
Use subagents/workers where available, but only with clear ownership and disjoint write scopes. Good lanes:

- storage/migrations
- adapter framework
- individual source importers
- search/summary/note CLI
- redaction/sync
- dream runner/assimilation
- docs/E2E/release verification

Per-Ticket Definition Of Done:
A ticket is Done only when:

- Linear acceptance criteria are satisfied or explicitly refined in a Linear comment.
- Relevant specs/docs/ADRs are updated.
- Tests or fixtures cover the behavior, including failure cases where meaningful.
- Existing raw retrieval contracts remain intact.
- CLI output rules are preserved: machine-readable JSON on stdout, human diagnostics on stderr.
- Required verification passes, or any skipped command has a concrete reason and follow-up.
- A brief adversarial review has been performed for risky changes.
- Linear is updated with what changed, commands run, residual risks, and next dependency.
- Work is committed cleanly, with unrelated user changes preserved.

Whole-MVP Definition Of Done:
The MVP is Done only when:

- All Todo MVP tickets are completed or explicitly closed/deferred with rationale.
- `mmr link` can set up a fresh project from a clean non-Git directory.
- `mmr sync` is idempotent and never destroys local history by default.
- `mmr status` accurately reports store/project/source/redaction/sync state.
- `projects`, `sessions`, `messages`, and `export` still retrieve raw history.
- `summary` replaces `remember`, with compatibility handled intentionally.
- `note`, `rg`, `search`, and `dream` work against the local store.
- Redaction blocks unsafe sync payloads by default.
- Fresh-host hydration from the remote store reconstructs usable local state.
- Learned memory is evidence-linked and cannot be applied without valid evidence refs.
- The full local E2E scenario passes without external credentials.
- Optional external smoke tests are gated by explicit env vars/credentials.
- `cargo fmt`, `cargo test`, benchmark test, clippy, and release build pass.
- README/docs explain exact MVP usage and known limitations.
- Final Linear/project update includes verification evidence and remaining risks.

Verification loop after meaningful code changes:
cargo fmt
cargo test
cargo test --test cli_benchmark -- --ignored --nocapture
cargo clippy --all-targets --all-features -- -D warnings
cargo build --release

MVP Constraints:
No `mmr init`.
No `store` namespace.
No public `learn`, `context`, `candidates`, `knowledge`, `promote`, or `reject` commands.
No org support.
No explicit GitHub repo argument in the happy path.
No destructive cleanup during `link` or `sync`.
Raw retrieval must remain useful.
Redaction must happen before sync by default.
Dreaming must write evidence-linked learned memory, not just summaries.

Session Continuity Requirements:
Continuity is part of the work, not an afterthought. Maintain durable state outside chat so the project can survive compaction, thread clearing, or handoff.

Create and maintain a repo-local continuity file, for example:
`docs/mmr-memory-fabric-progress.md`

Keep it updated after every meaningful step with:

- current branch
- last green commit
- active Linear ticket
- completed tickets
- exact next step
- current architecture decisions
- open blockers
- verification commands and results
- touched files/modules
- known risks
- han
